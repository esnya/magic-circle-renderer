import React from 'react';
import IconButton from './IconButton';

const ComponentRemoveButton = (props: Object) => (
    <IconButton name="trash-o" {...props} />
);
export default ComponentRemoveButton;
